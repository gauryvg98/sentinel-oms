[package]
name = "sentinel-binance"
description = "Binance USD-M futures: signing, REST, and the user data stream."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sentinel-domain = { path = "../sentinel-domain" }
sentinel-record = { path = "../sentinel-record" }
sentinel-types = { path = "../sentinel-types" }
sentinel-venue = { path = "../sentinel-venue" }

hmac = "0.12"
serde_json = "1"
sha2 = "0.10"
tungstenite = { version = "0.30", features = ["rustls-tls-webpki-roots"] }
ureq = "3"

[lints]
workspace = true
