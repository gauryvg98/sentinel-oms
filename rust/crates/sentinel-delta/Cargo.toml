[package]
name = "sentinel-delta"
description = "Delta Exchange India: signing, REST, and the user stream."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hmac = "0.13.0"
sentinel-domain = { path = "../sentinel-domain" }
sentinel-record = { path = "../sentinel-record" }
sentinel-types = { path = "../sentinel-types" }
sentinel-venue = { path = "../sentinel-venue" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sha2 = "0.11.0"
tungstenite = { version = "0.30.0", features = ["rustls-tls-webpki-roots"] }
ureq = "3.4.0"

[lints]
workspace = true
